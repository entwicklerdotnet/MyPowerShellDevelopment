## Container ist vorhanden aber es wurden keine Verknüpfungen erzeugt
# es muss ein gemapptes Veru´zeichnis existieren, alternativ muss mit docker cp kopiert werden
docker [PSCustomObject]@{
    Name = Value
}
